<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TripPlanner</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- Add custom styles for green switch -->
    <style>
        .form-check-input:checked {
            background-color: #198754;  /* Bootstrap's green color */
            border-color: #198754;
        }
        
        .form-check-input:focus {
            border-color: #198754;
            box-shadow: 0 0 0 0.25rem rgba(25, 135, 84, 0.25);
        }
/*-----------------------------------------------------------------------------------*/
        /* Add loading overlay styles */
        #loadingOverlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.95);
            z-index: 9999;
            justify-content: center;
            align-items: center;
        }

        .loading-content {
            text-align: center;
            background: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
/*-----------------------------------------------------------------------------------*/
    </style>
</head>
<body>
    <!-- Add loading overlay at the start of body -->
    <div id="loadingOverlay">
        <div class="loading-content">
            <div class="spinner-border text-success" style="width: 4rem; height: 4rem;" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <h3 class="text-success mt-4">Generating Your Trip Plan</h3>
            <p class="text-muted">Please wait while we create your personalized itinerary...</p>
        </div>
    </div>
<!-- ---------------------------------------------------------------------------------->
    <div class="row">
        <div class="col-6 offset-3 mt-5">
            <!-- Add novalidate to prevent browser validation -->
            <form action="/generate-trip" method="POST" class="validated-form" novalidate>
                <h1 class="text-center mb-4">Trip Planner</h1>
                <div class="mb-3">
                    <div class="mb-3">
                        <label for="country" class="form-label">Cities</label>
                        <input type="text" class="form-control" id="cities" name="cities" placeholder="Enter your cities" required>
                        <div class="valid-feedback">Looks good!</div>
                        <div class="invalid-feedback">Please input a city.</div>
                    </div>
                    <div class="mb-3">
                        <label for="duration" class="form-label">Duration</label>
                        <input type="number" class="form-control" id="duration" name="duration" placeholder="Enter how many days" required>
                        <div class="valid-feedback">Looks good!</div>
                        <div class="invalid-feedback">Please input a duration.</div>
                    </div>
                    <div class="mb-3">
                        <label for="interests" class="form-label">Interests</label>
                        <input type="text" class="form-control" id="interests" name="interests" placeholder="Enter interests (comma separated)" required>
                        <div class="valid-feedback">Looks good!</div>
                        <div class="invalid-feedback">Please input interests.</div>
                    </div>
                    <div class="mb-3">
                        <label for="budget" class="form-label">Budget</label>
                        <select class="form-select" id="budget" name="budget" required>
                            <option value="" selected disabled>Select your budget</option>
                            <option value="budget">Budget</option>
                            <option value="moderate">Moderate</option>
                            <option value="luxury">Luxury</option>
                        </select>
                        <div class="valid-feedback">Looks good!</div>
                        <div class="invalid-feedback">Please select a budget.</div>
                    </div>
                    
                    <!-- Add Hidden Gems Toggle Switch -->
                    <div class="mb-3 form-check form-switch mt-4">
                        <input class="form-check-input" type="checkbox" id="includeHiddenPlace" name="includeHiddenPlace" checked>
                        <label class="form-check-label" for="includeHiddenPlace">
                            <i class="bi bi-gem me-2"></i>Suggest Hidden Place
                        </label>
                        <div class="form-text text-muted">Get recommendations for lesser-known spots that tourists usually miss</div>
                    </div>
                </div>
                <div class="mb-4">
                    <button type="submit" class="btn btn-success">Generate Trip Plan</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/validateForm.js"></script>
    <script>
        const loader = document.getElementById("loadingOverlay");
        const form = document.querySelector(".validated-form");
        window.addEventListener("load", function() {
            loader.style.display = "none";
        });
        //show loader on form submit
        form.addEventListener("submit", function() {
            if(this.checkValidity()) {
                loader.style.display = "flex";
            } else{
                e.preventDefault();
                e.stopPropagation();
                this.classList.add('was-validated');
            }
        });
        window.onpageshow = function(event) {
            if (event.persisted) {
                loader.style.display = "none";
            }
        };
    </script>
</body>
</html>